# PI Engine Configuration
PI_ENGINE_BASE_URL=http://localhost:11434 # Base URL for the local LLM provider
PI_ENGINE_API_KEY=none # API Key (if required by provider, otherwise use 'none')
PI_ENGINE_MODEL_ID=gemma2 # Model ID to use for orchestration
